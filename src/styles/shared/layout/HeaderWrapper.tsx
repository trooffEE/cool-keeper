import styled from 'styled-components'

import colors from '@/styles/colors/colors'

const HeaderWrapper = styled.header`
  padding: 5px 10px;
  border-bottom: 2px solid ${colors.black.secondary};
`

export default HeaderWrapper